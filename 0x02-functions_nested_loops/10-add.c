#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers and returnsthe result
 *
 * @i: number being added
 *
 * @k: number being added
 *
 * Return: Always 0.
 *
 */
int add(int i, int k)
{
	return (i + k);
}
